# BandNames
random band name generator 

To run my program in Linux cd into the directory the project is in then run the command python3 ./BandNameGenerator.py
After you hit enter a random band name will be dispayed. To genreate a new one run the above command again.  
